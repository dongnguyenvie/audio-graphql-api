import helper from '../../../core/common/helper'

const FIELD = 'role'
/**
 * async (root, args, context, info)
 */
export default {
  Role: {
    // id: async (docs, args) => {
    //   return docs._id
    // }
  }
}
