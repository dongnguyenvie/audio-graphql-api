export default {
    // async (root, args, context, info)
    User: {
        //     roles: async (UserField, args, { models }, info) => {
        //       const user = await UserField.populate('roles')
        //       return user.roles
        //     }
    }
}
